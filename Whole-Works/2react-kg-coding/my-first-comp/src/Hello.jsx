// But for single component we don't recomend it this exporting

export default function Hello() {
  let age = 22;
  let myName = 'Abdul Haseeb';
  return (
    <div>
      <p >Hello I am {myName} and my age is {age}</p>
    </div>

  )
}

